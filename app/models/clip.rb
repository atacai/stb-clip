class Clip < ApplicationRecord
	def generate
		# idea for OOP :
		# put all parameters, example: status (on / off), sat, bri, and hue
		# use 'generate' function to call the api and pass all parameters to api
	end
end
